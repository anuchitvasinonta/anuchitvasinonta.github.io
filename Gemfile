source "https://rubygems.org"

# Pinned for compatibility with Ruby 2.6 (system Ruby on older macOS).
# To use the latest gems, upgrade Ruby first — see README below.
gem "jekyll", "~> 4.2.2"
gem "ffi", "1.15.5"           # ffi 1.16+ requires Ruby >= 3.0
gem "minima", "~> 2.5"

group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-feed"
end

# Required on macOS with older Ruby
gem "webrick", "~> 1.7"
